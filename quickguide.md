# pwnana quickguide

```
pwnana [length] [count] [-d] [-s]
```

| 例 | 説明 |
|---|---|
| `pwnana` | 16文字 × 1個 |
| `pwnana 20` | 20文字 × 1個 |
| `pwnana 20 5` | 20文字 × 5個 |
| `pwnana -d` | 16文字、数字1つ含む |
| `pwnana -s` | 16文字、記号1つ含む |
| `pwnana 24 -d -s` | 24文字、数字+記号 |

- `length` の最小値は 12 (それ以下を指定しても 12 になる)
- 数字・記号は leet 置換で挿入される (例: `a→@`, `e→3`, `o→0`)
- デフォルト可読性ルールとして大文字 `O` / `I` は生成しない
